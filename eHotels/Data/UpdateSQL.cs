namespace Data
{
    //all parameters are columnn names
    static class UpdateSQL
    {
        //public static readonly string UpdateHotel="UPDATE "+Utils.LoadColumns(TableColumnsAndTypes.Hotel)+" WHERE id=@id";
    }
}